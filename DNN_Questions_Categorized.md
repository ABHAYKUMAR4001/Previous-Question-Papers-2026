# DNN (Deep Neural Networks) - Comprehensive Exam Questions Categorized by Topic

**Papers Covered:**
- 2020 EC3 Regular (50% weightage)
- 2020 EC3 Makeup (50% weightage)
- 2022 EC3 Regular (30 marks)
- 2022 EC3 Makeup (30 marks)
- 2023 EC3 Regular (30 marks)
- 2024 EC3 Regular (35 marks) - Sept 2024
- 2024 EC3 Makeup (35 marks) - Oct 2024

---

## 1. CNN (Convolutional Neural Networks) - Architecture, Parameters & Computations

### Q1 [2020 Makeup, 10M]
A. Refer to the following code snippet of a CNN implementation using Keras:
```python
classifier = Sequential()
classifier.add(Conv2D(filters=16, kernel_size=(4,4), strides=(2,2), input_shape=(32,32,3), activation='relu'))
classifier.add(MaxPooling2D(pool_size=(3,3), strides=(2,2)))
classifier.add(Conv2D(filters=10, kernel_size=(3,3), padding='same', activation='relu'))
classifier.add(MaxPooling2D(pool_size=(3,3), strides=(1,1)))
classifier.add(Flatten())
classifier.add(Dense(units=64, activation='relu'))
classifier.add(Dense(units=1, activation='sigmoid'))
classifier.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])
```
a) Calculate the dimension of the input matrix at each step of the network.
b) What would be the difference in the dimension of the input vector of the fully connected layer had we not performed convolution steps?
c) Above code uses "adam" optimizer. What benefit does this over traditional SGD?
d) You want to train a picture classifier for your face. What is the key data pre-processing step needed? What are the disadvantages?

### Q2 [2020 Regular, 10M - Part B]
Refer to the VGG16 code:
```python
from tensorflow.keras.applications.vgg16 import VGG16
from tensorflow.keras.models import Model
model = VGG16()
model.layers.pop()
model = Model(inputs=model.inputs, outputs=model.layers[-2].output)
```
a) If we add Batch Normalization after every convolution layer in the modified VGG16 model, what will be the total number of additional trainable parameters (betas and gammas)?
b) What will be the total number of non-trainable parameters (means and variances) in the first 3 Batch Normalization layers?

### Q3 [2023 Regular, 8M]
Two historians want to classify images of historical objects into 3 classes. For each layer of a CNN classifier, calculate the number of weights, number of biases and the size of the associated feature maps.
Architecture: INPUT (128x128x3) -> CONV-9-32 -> POOL-2 -> CONV-5-64 -> POOL-2 -> CONV-5-64 -> POOL-2 -> FC-64 -> FC-3
(Padding=0, Stride=1 for Conv; Stride=K, Padding=0 for Pool)

### Q4 [2023 Regular, 6M]
Consider a simplified NiN architecture with one NiN block followed by global average pooling and softmax. The NiN block consists of: 1x1 conv with 20 filters, three 3x3 conv layers with 50 filters each, two 5x5 conv layers with 20 filters each, ReLU after each layer. Input: 256x256 greyscale.
(a) Number of parameters in one NiN block.
(b) Dimensions of the output feature map after the NiN block.
(c) Total number of parameters in the entire NiN architecture.

### Q5 [2022 Regular, 5M]
Calculate the number of parameters for a VGG-like CNN:
```
Input: 224 x 224 x 3
Layer 1: 64 filters (3 x 3), stride 1, padding 1
Layer 2: 64 filters (3 x 3), stride 1, padding 1
MaxPool: 2x2, stride 2
Layer 3: 128 filters (3 x 3), stride 1, padding 1
Layer 4: 128 filters (3 x 3), stride 1, padding 1
```
Calculate parameters for each layer.

### Q6 [2024 Regular, 4M - Part Q4a]
A medical doctor wants to identify a rare disease from MRI images. 75% are labeled. The doctor provides a lot of MRI images.
a) Choose the most suitable type of learning paradigm and justify.
b) Suggest a suitable DNN architecture and justify.
c) Suggest a suitable loss function.
d) How would you handle the class imbalance?

---

## 2. Convolution Operations (Transposed, Dilated, Stacked Convolutions)

### Q1 [2020 Makeup, 8M]
A. Consider a 2x2 convolution operator O = [[-1,1],[-1,1]]. Output is generated by stacking two such operators.
a) What is the size of the equivalent operator V applied once?
b) Show the elements Vij of equivalent operator V.

B. Assuming stride=1 and padding=0, what is the size of transposed convolution of a 2x2 kernel [[0,2],[1,3]] with the input [[0,2],[1,3]]? Show the result.

C. Consider Google LeNet inception block with 1x1 convolutions of depth 200, 100, 20, 75. Input: 20x20x500.
a) What is the size of output volume? What is the padding used during 1x1, 3x3 and 5x5 convolution?
b) How many parameters in this block?

### Q2 [2020 Regular, 10M]
A. Consider a 1x3 convolution operator o = [-1, 2, -1]. Output is generated by stacking two such operators.
a) What is the size of the equivalent operator O?
b) Show elements O(i,j) of equivalent operator O.

B. Consider a 3x3 operator and 7x7 input.
a) What will be the output size for 2-dilated convolution with padding 0 and stride 1?
b) What will be the value at position (2,1) in the output plane?

C. 1x1 operators on 50x50 images of depth 200, first output depth 50, then 5x5 operators output depth 75.
a) What is the padding size used in first step and last step?
b) How many multiplication operations are needed?

---

## 3. RNN (Recurrent Neural Networks) - Design & Computation

### Q1 [2020 Makeup, 10M]
Design an RNN that outputs a parity bit for binary sequences of arbitrary length. Inputs are binary from right to left; output 1 when number of '0's seen so far is even. RNN has 1 input, 2 hidden units, 1 output. All use hard threshold activation.
A. Specify state representations for the FSM in terms of hidden node outputs.
B. Express current hidden states as Boolean functions of past hidden states and current input.
C. Specify weight matrices U(2x1), v(1x2), W(2x2), bias bh(2x1) and scalar by.

### Q2 [2020 Regular, 9M]
Design an RNN that adds binary numbers of arbitrary length. Inputs are binary sequences (LSB first, padded with zeros). RNN has 2 inputs, 3 hidden units, 1 output. All use hard threshold.
A. Choose state representations for the FSM.
B. Specify weight matrices U(3x2), v(1x3), W(3x3), bias bh(3x1) and scalar by.

### Q3 [2023 Regular, 6M]
Design a single hidden layer RNN that outputs the moving sum of difference of two input real sequences. Example: p={0.6,0.4,0.3,1.2,-1.2}, q={-1.2,1.2,0.3,0.4,0.6} produces output {1.8,1.0,1.0,1.8,0}. All nodes use linear activation g(x)=x.
(a) What is the minimum number of hidden nodes required?
(b) Compute W, U, V, bh and by.

### Q4 [2024 Regular, 8M]
a) Compute the outputs in each timestep and the state after timestep=3 for the Vanilla RNN:
X=[1,1,0]^T, W=[0.2,0.3,0.8]^T, U=[0.5,0.6,0.2]^T, V=[0.4,0.2,0.1]^T. Assume biases as zeros.

b) A company automates report generation from videos of ultrasound scans of fetal hearts. Videos are 36 frames, each 360x240 grayscale.
a) Suggest suitable type of RNN and justify.
b) Compute the depth of the RNN suggested.
c) Should LSTM, GRU, or Bidirectional RNN be used? Justify.

### Q5 [2024 Makeup, 4M + 4M + 2M]
a) Code snippet:
```python
Model = Sequential()
Model.add(SimpleRNN(4, input_shape=(4,6), activation=activation[0]))
Model.add(Dense(1, activation="Sigmoid"))
```
Draw the architecture and calculate number of parameters.

b) If Xt, ht, Ct are of dimensions 4, 5, 5 respectively, find the number of parameters to be trained at ft (forget gate) in an LSTM. Justify: "In the above architecture sigma can be replaced with tanh to obtain the optimal architecture."

---

## 4. LSTM & GRU - Parameter Calculation

### Q1 [2023 Regular, 5M]
A deep learning researcher designs an LSTM-based neural network for NLP. Single-layer LSTM with input size=210, hidden size=28, not bidirectional. Expected output is a 212 vector. Calculate total number of parameters. Show gate-wise calculations.

### Q2 [2024 Makeup, 4M + 2M]
If Xt, ht, Ct are of dimensions 4, 5, and 5 respectively:
i) Find the number of parameters at the forget gate (ft). Show calculations.
ii) Justify: "In the above architecture sigma can be replaced with tanh to obtain the optimal architecture."

---

## 5. Attention Mechanisms & Transformers

### Q1 [2023 Regular, 5M]
Compute dimensions of W^Q, W^K, W^V, W^O if input dimension d=2048, query/key dimensions dk=512, value dimensions dv=1024, number of heads h=4. Compute dimensions of the attention vector.

### Q2 [2024 Regular, 6M + 2M + 2M]
a) Given text sequence with 3 words, each as 2-d vector in matrix X. Show step-by-step calculations for computing scaled dot-product self-attention scores using Wq, Wk, Wv matrices.

b) i) Mention and justify different types of attention mechanisms used in Transformer architecture.
   ii) Justify selection of Bahdanau attention and its disadvantages. Is there any alternative?

### Q3 [2024 Makeup, 8M]
Given Q1=[1,0], Q2=[0,1], K1=[1,1], K2=[0,1], V1=[2,1], V2=[0,3]:
- Calculate attention scores and attention-weighted outputs for both queries using dot-product attention and softmax.

---

## 6. Optimization (Gradient Descent, Momentum, Adam, Learning Rate)

### Q1 [2020 Makeup, 10M - Q3]
A. What is a saddle point? Advantage/disadvantage of SGD with saddle points?
B. Two cost curves: which is batch GD vs mini-batch GD? Explain.
C. What caused sudden drop in cost during training? (Learning rate decay)
D. Error function E(w1,w2). Given initial point, compute:
   a) Values that minimize E, minimum E value
   b) (w1,w2) at t+1 using standard gradient descent
   c) (w1,w2) at t+1 using momentum-based gradient descent
   d) (w1,w2) at t+1 using Nesterov's accelerated gradient
   e) Which technique gives highest reduction in E?

### Q2 [2020 Regular, 9M - Q3]
A. Computational graph: Calculate partial derivatives in backward pass.
B. Is it necessary to randomize order of training data in full batch gradient descent? Explain.
C. When is GD preferred over SGD, and vice-versa?

### Q3 [2024 Makeup, 2M - Q1g]
Compare momentum based gradient descent algorithm against RMSprop.

---

## 7. ANN/MLP (Perceptron Networks, Feedforward Networks)

### Q1 [2020 Regular, 10M - Q1A]
Refer to partial TensorFlow code for ANN:
```python
a = tf.placeholder(tf.float32, shape=[None, 16])
b = tf.placeholder(tf.float32, shape=[None, 3])
...
cost = tf.reduce_mean(tf.nn.softmax_cross_entropy_with_logits(fl, b))
optimizer = tf.train.AdamOptimizer(learning_rate=0.5).minimize(cost)
```
a) Which activation function is used in output layer? Why? What if sigmoid used instead?
b) Calculate output values given input vector, weight matrix, and bias vector.

### Q2 [2020 Makeup, 8M - Q2]
A. Network with ReLU activation (bias T): find output for x=1.0, 2.5, 4, 8.
B. Network outputs Y='1' when number of 1's in input (x1,x2) is zero or even. Specify weights and biases.

### Q3 [2020 Regular, 9M - Q2]
A. What is minimum number of hidden nodes in single hidden layer for even parity generator (5-bit)?
B. Minimum hidden nodes with multiple hidden layers? How many layers?
C. Total weights for single hidden layer implementation?
D. Total weights for multiple hidden layer implementation?

### Q4 [2024 Makeup, 10M - Q1]
Code snippet:
```python
dnnModel = Sequential()
dnnModel.add(Dense(60, activation='tanh', input_shape=(64*64,)))
dnnModel.add(Dense(40, activation='relu'))
dnnModel.add(Dense(20, activation='relu'))
dnnModel.add(Dense(10, activation='relu'))
dnnModel.add(Dense(4, activation='softmax'))
```
a) Number of hidden layers
b) Number of categories classified
c) Size of input vector
d) Draw the network
e) When to use sigmoid vs softmax in output layer?
f) Compare tanh and ReLU in terms of gradients
g) Compare momentum-based GD vs RMSprop

### Q5 [2024 Makeup, 3M - Q4c]
Consider a fully connected 3-layered feedforward NN with 5 neurons per layer (except output with 4). Draw architecture and show total learnable parameters at each layer.

---

## 8. Regularization & Overfitting

### Q1 [2024 Regular, 4M]
Two friends experiment with an overfitting DNN:
- Mr. Raju adds dropout + batch normalization
- Mr. Robert adds more layers + early stopping due to hardware limitations
Assess whether their decisions help them proceed in the right direction.

---

## 9. GANs (Generative Adversarial Networks)

### Q1 [2020 Makeup, 4M - Q6]
Consider graph of generator cost vs discriminator output D(G(z)):
A. Early in training, is D(G(z)) closer to 0 or 1? Why?
B. Two cost functions: which would you choose to train? Why?
C. What types of convolution are used in pooling layers of CNN-based GANs?
D. Why is KL divergence used for regularization in variational autoencoders?

---

## 10. NLP (Natural Language Processing)

### Q1 [2020 Makeup - Q1B, Q1C]
B. Which is NOT a pre-processing technique? (Stemming, lowercase, removing punctuations, POS tagging)

C. BLEU Score:
a) What does the "weights" parameter in corpus_bleu signify?
b) Given specific references and hypothesis, what will be the BLEU score output?

---

## 11. Transfer Learning & Applications

### Q1 [2024 Regular, 7M - Q4]
a) A medical doctor needs to identify a rare disease from MRI images with only 75% labeled data:
   - Choose suitable learning paradigm
   - Suggest DNN architecture
   - Suggest loss function
   - Handle class imbalance

b) Propose a DNN architecture to predict weather for next 3 days based on past 7 days data. Each day has: temperature, humidity, wind speed (128-dimensional encoding each).
   - Draw architecture
   - Compute number of parameters
   - Can future weather predictions affect past conditions?

---

## 12. Neural Architecture Search (NAS)

### Q1 [2024 Makeup, 2M + 2M]
a) Briefly explain how One-Shot Architecture Search works.
b) Discuss ONE potential advantage and ONE potential disadvantage of using this method for performance estimation in NAS.

---

## 13. Batch Normalization

### Q1 [2020 Regular, Part of Q1B]
If Batch Normalization is added after every convolution layer in modified VGG16:
a) Total additional trainable parameters (betas, gammas)?
b) Total non-trainable parameters (means, variances) in first 3 BN layers?

---

## 14. Backpropagation & Computational Graphs

### Q1 [2020 Regular, 9M - Q3A]
Computational graph for g(x) = tanh(x). Calculate partial derivatives in backward pass: dg/de, dg/df, dg/dc, dg/dd, dg/da, dg/db, dg/dx.

### Q2 [2022 Makeup, 5M]
Compute the forward pass output and backward pass gradients of a simple 2-layer fully connected network for given inputs and weights.

---
